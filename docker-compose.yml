services:
  openclaw-admin:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://${SERVER_IP}:4000
        NEXT_PUBLIC_API_TOKEN: ${API_TOKEN}
    restart: unless-stopped
    ports:
      - "3000:3000"
